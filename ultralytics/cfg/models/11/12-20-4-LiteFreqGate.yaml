# 🧠 预期参数量: 2.8M - 2.9M
# 💻 预期 GFLOPs: 10.0 - 10.5
# 🎯 目标: Tricycle mAP ≥ 0.20

nc: 6
scales:
  n: [0.50, 0.25, 1024]

# ==================== Backbone ====================
backbone:
  - [-1, 1, Conv, [64, 3, 2]]           # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]          # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]   # 2
  - [-1, 1, Conv, [256, 3, 2]]          # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]   # 4
  - [-1, 1, Conv, [512, 3, 2]]          # 5-P4/16
  - [-1, 2, C3k2, [512, True]]          # 6
  - [-1, 1, Conv, [1024, 3, 2]]         # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]         # 8
  - [-1, 1, SPPF, [1024, 5]]            # 9
  - [-1, 2, C2PSA, [1024]]              # 10

# ==================== Head ====================
head:
  # === 上采样路径 ===
  - [-1, 1, DySample, [2, 'lp']]        # 11 (P5->P4)
  - [[-1, 6], 1, Concat, [1]]           # 12
  - [-1, 2, C3k2, [512, False]]         # 13 (P4 融合)

  - [-1, 1, DySample, [2, 'lp']]        # 14 (P4->P3)
  - [[-1, 4], 1, Concat, [1]]           # 15
  - [-1, 2, C3k2, [256, False]]         # 16 (P3 语义流)

  # === Detail Branch ===
  - [1, 1, Conv, [128, 3, 2]]           # 17 (Detail: P2->P3)

  # === LiteGate 融合 ===
  - [[16, 17], 1, LiteFrequencyGate, [256]]  # 18 (融合 P3_Sem + Detail)

  # === 下采样输出路径 ===
  - [18, 1, Conv, [256, 3, 2]]          # 19 (P3->P4)
  - [[-1, 13], 1, Concat, [1]]          # 20
  - [-1, 2, C3k2, [512, False]]         # 21 (P4 输出)

  - [-1, 1, Conv, [512, 3, 2]]          # 22 (P4->P5)
  - [[-1, 10], 1, Concat, [1]]          # 23
  - [-1, 2, C3k2, [1024, True]]         # 24 (P5 输出)

  # === Detect 头 ===
  - [[18, 21, 24], 1, Detect, [nc]]     # 25 
