# YOLOv11n-GSConv-Full-Optimization
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone (不变)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] 
  - [-1, 1, Conv, [128, 3, 2]] 
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] 
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] 
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] 
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] 
  - [-1, 2, C2PSA, [1024]] 

# Head
head:
  # UpSample & Concat (不变)
  - [-1, 1, DySample, [2, 'lp']] 
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] 

  - [-1, 1, DySample, [2, 'lp']] 
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16

  # === Detail Branch ===
  - [1, 1, RFAConv, [128]] 
  - [-1, 1, GSConv, [128, 3, 2]] # 18

  # === Gate ===
  - [[16, 18], 1, FrequencyGate, [128, 256]] # 19 (这是 P3 小目标特征，80x80)

  # === 📉 下采样到 P4 ===
  # 注意：这里输入必须显式指向 19
  - [19, 1, GSConv, [256, 3, 2]] # 20 (P3 -> P4)

  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 22 (P4 特征)

  # === 📉 下采样到 P5 ===
  - [-1, 1, GSConv, [512, 3, 2]] # 23 (P4 -> P5)

  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 25 (P5 特征)

  # 🚨🚨🚨【关键修改】🚨🚨🚨
  # 输入必须是 [19, 22, 25]
  # 19: Small Object (Gate Output)
  # 22: Medium Object
  # 25: Large Object
  - [[19, 22, 25], 1, Detect, [nc]] 
