# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# SGE-YOLO (Semantic-Guided Edge-Fusion)
# --------------------------------------------------------
# 核心逻辑: "查漏补缺"
# 1. Backbone: 稳如老狗的 C3k2 (保 mAP)。
# 2. Detail Branch: FaRFA-Lite (RFAConv 64通道) (保 Tricycle 且降 FLOPs)。
# 3. Fusion: SGEFusion (差分 + HWD + DySample) (核心创新)。
# --------------------------------------------------------

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (细节流源头)
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head
head:
  # === DySample 上采样 ===
  - [-1, 1, DySample, [2, 'lp']] # 11
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, DySample, [2, 'lp']] # 14
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义流 - 256通道)

  # === 🚀 Detail Branch (FaRFA Lite) ===
  # 重点: 输出通道改为 64！(原来是128)
  # 目的: 大幅降低后续 SGEFusion 的计算量
  - [1, 1, RFAConv, [64]] # 17 
  
  # 注意: 这里不再需要单独的 HWD_Down 了，因为 HWD 逻辑已经被集成进 SGEFusion 内部了
  # 这又省了一层计算

  # === 🚀 SGE Fusion (核心核武器) ===
  # 输入: [P3(256), Detail(64)]
  # 作用: 计算 P2-DySample(P3) 的差分，并通过 HWD 压回 P3
  - [[16, 17], 1, SGEFusion, [64, 256]] # 18 (args: c_detail, c_out)

  # === 输出路径 ===
  - [18, 1, Conv, [256, 3, 2]] 
  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 21

  - [-1, 1, Conv, [512, 3, 2]] 
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 24

  - [[18, 21, 24], 1, Detect, [nc]]