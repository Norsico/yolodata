# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# FaRFA-Rep-Plus-YOLO
# --------------------------------------------------------
# 策略: 
# 1. Detail Branch 源头改为 Layer 2 (C3k2 输出)，特征更纯净。
# 2. 下采样使用 RepConv，并显式指定 Padding=1 以修复报错。
# --------------------------------------------------------

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (Raw)
  - [-1, 2, C3k2, [256, False, 0.25]] # 2-P2/4 (Refined -> 最佳源头)

  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head
head:
  # === DySample ===
  - [-1, 1, DySample, [2, 'lp']] # 11
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, DySample, [2, 'lp']] # 14
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义流)

  # === 🚀 [升级] Detail Branch ===
  
  # 17: RFAConv 
  # 来源: [2] (Backbone P2 的 C3k2 输出)
  - [2, 1, RFAConv, [128]] 
  
  # 18: RepConv 下采样
  # 🚀 关键修复: 加上第4个参数 1 (Padding)
  - [-1, 1, RepConv, [128, 3, 2, 1]] 

  # === Gate ===
  # 19
  - [[16, 18], 1, FrequencyGate, [128, 256]] 

  # === 输出路径 ===
  - [19, 1, Conv, [256, 3, 2]] 
  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 22

  - [-1, 1, Conv, [512, 3, 2]] 
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 25

  - [[19, 22, 25], 1, Detect, [nc]]