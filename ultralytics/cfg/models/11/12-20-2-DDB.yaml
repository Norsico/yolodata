
# =============================================================================
# YOLOv11n - QSD + Gate (The "Best of Both Worlds")
# Combination:
# 1. DBB_Lite: Super-charged feature extraction (from QSD paper).
# 2. FrequencyGate: Intelligent feature selection (your best trick).
# =============================================================================

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone (Standard)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (Source: 160x160)
  - [-1, 2, C3k2, [256, False, 0.25]] # 2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]] # 4
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]] # 6
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]] # 8
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head
head:
  # === 🏗️ DBB Detail Branch (你的强力新引擎) ===
  # 训练时多路并行学习，提取极小目标特征
  - [1, 1, DBB_Lite, [128, 3, 1]] # 11 (160x160)
  - [-1, 1, DBB_Lite, [128, 3, 2]] # 12 (80x80)

  # === Standard P5 -> P4 ===
  - [10, 1, DySample, [2, 'lp']] # 13
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 15

  # === P4 -> P3 (Main Semantic Stream) ===
  - [-1, 1, DySample, [2, 'lp']] # 16
  - [[-1, 4], 1, Concat, [1]] # Cat with Backbone P3
  - [-1, 2, C3k2, [256, False]] # 18 (Main P3 Features)

  # === ⛩️ The Fusion: DBB meets Gate ===
  # Input 1: Main Semantic P3 (Layer 18) [256ch]
  # Input 2: DBB Detail P3 (Layer 12) [128ch]
  # Gate 会自动处理通道对齐 (256 vs 128)
  - [[18, 12], 1, FrequencyGate, [256, 128]] # 19

  # === Output Path ===
  - [19, 1, Conv, [256, 3, 2]] # 20
  - [[-1, 15], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 22

  - [-1, 1, Conv, [512, 3, 2]] # 23
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 25

  - [[19, 22, 25], 1, Detect, [nc]] # 26