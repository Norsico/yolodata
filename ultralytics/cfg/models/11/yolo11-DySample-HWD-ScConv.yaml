# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# YOLO11-WT-ScConv + DySample
# 核心创新: Backbone 使用 (SCConv清洗 + WT双流) 进行下采样

# Parameters
nc: 6 # SODA10M
scales: 
  n: [0.50, 0.25, 1024] 

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2 (第一层还是保留普通Conv)
  
  # ⬇️ [创新点] P2 下采样: 替换为 WT_ScConv
  # 1. SCConv 抑制路面/绿化带噪声
  # 2. WT 保留三轮车轮廓
  - [-1, 1, WT_ScConv, [128]] # 1-P2/4

  - [-1, 2, C3k2, [256, False, 0.25]]
  
  # ⬇️ [创新点] P3 下采样
  - [-1, 1, WT_ScConv, [256]] # 3-P3/8

  - [-1, 2, C3k2, [512, False, 0.25]]
  
  # ⬇️ [创新点] P4 下采样
  - [-1, 1, WT_ScConv, [512]] # 5-P4/16

  - [-1, 2, C3k2, [512, True]]
  
  # ⬇️ [创新点] P5 下采样
  - [-1, 1, WT_ScConv, [1024]] # 7-P5/32

  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head (保持 DySample 不变)
head:
  - [-1, 1, DySample, [2, 'lp']] 
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, DySample, [2, 'lp']]
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2, [256, False]] # 16 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2, [512, False]] # 19 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2, [1024, True]] # 22 (P5/32-large)

  - [[16, 19, 22], 1, Detect, [nc]]