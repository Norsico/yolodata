# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# FaRFA-GhostV3-YOLO
# --------------------------------------------------------
# 目标: 冲击 mAP50 > 0.57，同时 GFLOPs < 25
# 1. Backbone: 使用 C2f_GhostV3。GhostNetV3 引入了重参数化思想，
#    通过训练时的多分支(1x5, 5x1)增强特征，解决了轻量级网络特征弱的问题。
# 2. Detail Branch: FaRFA (RFAConv + HWD)。三轮车检测的定海神针。
# 3. Head: Gate 融合。
# --------------------------------------------------------

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone (GhostV3 重构)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (细节流源头)
  
  # 🚀 [替换] C3k2 -> C2f_GhostV3
  # 使用 GhostNetV3 模块，利用 Rep 机制增强特征
  - [-1, 2, C2f_GhostV3, [256, False, 0.25]] # 2
  
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  
  # 🚀 [替换] C3k2 -> C2f_GhostV3
  - [-1, 2, C2f_GhostV3, [512, False, 0.25]] # 4
  
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  
  # 🚀 [替换] C3k2 -> C2f_GhostV3
  - [-1, 2, C2f_GhostV3, [512, True]] # 6
  
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  
  # 🚀 [替换] C3k2 -> C2f_GhostV3
  - [-1, 2, C2f_GhostV3, [1024, True]] # 8
  
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head (保持 FaRFA 架构)
head:
  # === DySample 上采样 ===
  - [-1, 1, DySample, [2, 'lp']] # 11
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 13 (Head 处保留 C3k2 求稳)

  - [-1, 1, DySample, [2, 'lp']] # 14
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义流)

  # === 🚀 Detail Branch (FaRFA) ===
  # 目前最强的组合：RFAConv (注意力) + HWD (边缘)
  - [1, 1, RFAConv, [128]] # 17
  - [-1, 1, HWD_Down, [128]] # 18

  # === Frequency Gate ===
  - [[16, 18], 1, FrequencyGate, [128, 256]] 

  # === 输出 ===
  - [19, 1, Conv, [256, 3, 2]] 
  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 22

  - [-1, 1, Conv, [512, 3, 2]] 
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 25

  - [[19, 22, 25], 1, Detect, [nc]]