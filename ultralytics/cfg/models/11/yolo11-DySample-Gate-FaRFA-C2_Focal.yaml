# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# FaRFA-Focal-YOLO (The Final Weapon)
# --------------------------------------------------------
# 1. Detail: FaRFA (RFAConv+HWD) 128ch -> 锁死三轮车纹理。
# 2. Head: Gate -> 解决融合歧义。
# 3. P5 Enhancement: C2_Focal -> 替代 C2PSA。
#    利用 Focal Modulation 捕捉长距离上下文，解决 P5 层"只见树木不见森林"的问题。
# --------------------------------------------------------

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone (C3k2 原生)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  
  # 🚀 [替换] C2PSA -> C2_Focal
  # 在最深层(P5)引入焦点调制，增强全局上下文感知
  - [-1, 2, C2_Focal, [1024]] # 10

# Head (FaRFA 满血版)
head:
  # === DySample ===
  - [-1, 1, DySample, [2, 'lp']] # 11
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, DySample, [2, 'lp']] # 14
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义)

  # === 🚀 Detail Branch (FaRFA 128) ===
  # 既然你发现 RFAConv 提升明显，我们就不砍通道了，保持 128
  - [1, 1, RFAConv, [128]] # 17 
  - [-1, 1, HWD_Down, [128]] # 18

  # === Gate ===
  - [[16, 18], 1, FrequencyGate, [128, 256]] # 19

  # === Output ===
  - [19, 1, Conv, [256, 3, 2]] 
  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 22

  - [-1, 1, Conv, [512, 3, 2]] 
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 25

  - [[19, 22, 25], 1, Detect, [nc]]
