# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# SDC-YOLO (Semantic-Detail Collaborative YOLO)
# --------------------------------------------------------
# 核心策略: 
# 1. 移除 FaRFA (HWD)，避免高频噪声破坏模糊三轮车的特征。
# 2. 使用 ScConv 清洗 P2，保留最原始的 RGB 细节。
# 3. 使用 SDC-Gate: 让门控机制同时看到语义和细节，解决"盲人摸象"问题。
# --------------------------------------------------------

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone (C3k2 原生)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head
head:
  # === DySample 上采样 ===
  - [-1, 1, DySample, [2, 'lp']] # 11
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, DySample, [2, 'lp']] # 14
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义)

  # === 🚀 Detail Branch (回归 ScConv) ===
  # FaRFA 证明对 Recall 有害，我们回归 ScConv
  # ScConv 负责把 P2 的大面积背景去掉，只把"有东西"的地方留下来
  - [1, 1, ScConv, [128]] # 17 

  # === 🚀 SDC_Gate (协同门控) ===
  # 输入: [P3(256), Detail(128)] -> 输出: 256
  # 这里 Gate 会同时看 P3 和 P2，决定保留哪些细节
  - [[16, 17], 1, SDC_Gate, [128, 256]] # 18

  # === 输出路径 ===
  - [18, 1, Conv, [256, 3, 2]] 
  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 21

  - [-1, 1, Conv, [512, 3, 2]] 
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 24

  - [[18, 21, 24], 1, Detect, [nc]]