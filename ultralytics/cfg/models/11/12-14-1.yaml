# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# FaRFA-EMA-YOLO (Final Edition)
# --------------------------------------------------------
# 1. Detail Branch: 源头改为 Layer 2 (C3k2)，配合普通 Conv 下采样。
#    -> 这是你验证过的 Tricycle 0.2295 的核心配置。
# 2. Neck: DySample + Gate 保持不变。
# 3. Head: 引入 EMA (Official Code) 增强 P3/P4/P5。
#    -> 修复 Pedestrian/Cyclist 的微小掉点。
# --------------------------------------------------------

# Parameters
nc: 6 
scales: 
  n: [0.50, 0.25, 1024] 

# Backbone (C3k2 原生，不动)
backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (Raw)
  - [-1, 2, C3k2, [256, False, 0.25]] # 2-P2/4 (Refined -> 最佳源头)

  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Head
head:
  # === DySample 上采样 ===
  - [-1, 1, DySample, [2, 'lp']] # 11
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 13

  - [-1, 1, DySample, [2, 'lp']] # 14
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义流)

  # === 🚀 Detail Branch (最强形态) ===
  
  # 17: RFAConv (源头 [2])
  # 使用 Backbone P2 的 C3k2 输出，特征更扎实
  - [2, 1, RFAConv, [128]] 
  
  # 18: 普通 Conv 下采样 (Tricycle 救星)
  # [128通道, 3核, 2步长] - 不用 RepConv 也不用 HWD，回归朴素
  - [-1, 1, Conv, [128, 3, 2]] 

  # === Gate 融合 ===
  - [[16, 18], 1, FrequencyGate, [128, 256]] # 19 (Fused P3)

  # === Neck Output Path ===
  - [19, 1, Conv, [256, 3, 2]] # 20
  - [[-1, 13], 1, Concat, [1]] 
  - [-1, 2, C3k2, [512, False]] # 22 (P4 feat)

  - [-1, 1, Conv, [512, 3, 2]] # 23
  - [[-1, 10], 1, Concat, [1]] 
  - [-1, 2, C3k2, [1024, True]] # 25 (P5 feat)

  # ==========================================
  # 🚀 [升级] EMA Attention (官方代码版)
  # ==========================================
  
  # 注意: args 写 [] 即可，默认使用 factor=32
  # EMA 会自动获取输入通道数，并保持输出通道数不变
  
  - [19, 1, EMA, []] # 26 (Enhance P3)
  - [22, 1, EMA, []] # 27 (Enhance P4)
  - [25, 1, EMA, []] # 28 (Enhance P5)

  # Detect (输入变成增强后的层)
  - [[26, 27, 28], 1, Detect, [nc]]