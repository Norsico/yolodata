# 🧠 预期参数量: ~3.0 M
# 💻 GFLOPs: 8 G 
# 🎯 目标: Tricycle 回归 0.20+

nc: 6
scales:
  n: [0.50, 0.25, 1024]

# === Backbone (YOLOv11 原版) ===
backbone:
  - [-1, 1, Conv, [64, 3, 2]]
  - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]]
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]]
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]]
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]
  - [-1, 2, C2PSA, [1024]]

# === Head ===
head:
  - [-1, 1, DySample, [2, 'lp']]
  - [[-1, 6], 1, Concat, [1]]
  - [-1, 2, C3k2, [512, False]]

  - [-1, 1, DySample, [2, 'lp']]
  - [[-1, 4], 1, Concat, [1]]
  - [-1, 2, C3k2, [256, False]] # 16 (P3 语义流: 256通道)

  # === Detail Branch (回归 12-13-2 的配置) ===
  # 既然 Layer 2 太占显存且不好调，我们回退到 Layer 1 + RFAConv
  # RFAConv 在这里负责把 32 通道的信息尽量榨干
  - [1, 1, RFAConv, [128]]      # 17 (Layer 1 -> 128ch)
  - [-1, 1, Conv, [128, 3, 2]]  # 18

  # === 融合层 (SepFrequencyGate) ===
  # 输入: 256(Sem) + 128(Detail)
  # 输出: 256 (保持高通道数！)
  - [[16, 18], 1, SepFrequencyGate, [128, 256]] # 19

  # === 输出路径 ===
  - [19, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]]
  - [-1, 2, C3k2, [512, False]]

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]]
  - [-1, 2, C3k2, [1024, True]]

  # === Detect ===
  # ✅ 关键点: P3 输入是 19 (SepGate输出)，通道数是 256
  # 这保证了小目标检测头有足够的容量
  - [[19, 22, 25], 1, Detect, [nc]] 
