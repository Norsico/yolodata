# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics YOLO11n - HWD + DySample
# 组合改进: 
# 1. Backbone 下采样层替换为 Down_wt (Haar小波)，无损保留高频信息
# 2. Head 上采样层替换为 DySample (动态上采样)，内容感知恢复细节

# Parameters
nc: 6 # number of classes (SODA10M)
scales: 
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] 

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2  (第一层保留Conv，计算量考虑)
  
  # ⬇️ [改进 1] P2 下采样: Conv -> Down_wt (关键层！防止小目标在初期消失)
  # 原代码: - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 1, Down_wt, [128]] # 1-P2/4 

  - [-1, 2, C3k2, [256, False, 0.25]]
  
  # ⬇️ [改进 1] P3 下采样: Conv -> Down_wt
  # 原代码: - [-1, 1, Conv, [256, 3, 2]]
  - [-1, 1, Down_wt, [256]] # 3-P3/8

  - [-1, 2, C3k2, [512, False, 0.25]]
  
  # ⬇️ [改进 1] P4 下采样: Conv -> Down_wt
  # 原代码: - [-1, 1, Conv, [512, 3, 2]]
  - [-1, 1, Down_wt, [512]] # 5-P4/16

  - [-1, 2, C3k2, [512, True]]
  
  # ⬇️ [改进 1] P5 下采样: Conv -> Down_wt
  # 原代码: - [-1, 1, Conv, [1024, 3, 2]]
  - [-1, 1, Down_wt, [1024]] # 7-P5/32

  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head
head:
  # ⬇️ [改进 2] 上采样: nn.Upsample -> DySample (保持你之前的修改)
  - [-1, 1, DySample, [2, 'lp']] 

  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2, [512, False]] # 13

  # ⬇️ [改进 2] 上采样: nn.Upsample -> DySample (保持你之前的修改)
  - [-1, 1, DySample, [2, 'lp']]

  - [[-1, 4], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2, [256, False]] # 16 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2, [512, False]] # 19 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2, [1024, True]] # 22 (P5/32-large)

  - [[16, 19, 22], 1, Detect, [nc]] # Detect(P3, P4, P5)